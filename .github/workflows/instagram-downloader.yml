name: Instagram Reel Downloader

on:
  schedule:
    - cron: '0 12 * * *'  # Runs at 12:00 UTC every day
  workflow_dispatch:  # Allows manual triggering

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create output directory
      run: |
        mkdir -p ${{ secrets.BASE_OUTPUT_DIR }}
    
    - name: Run Instagram Downloader
      env:
        BASE_OUTPUT_DIR: ${{ secrets.BASE_OUTPUT_DIR }}
      run: |
        python src/downloader.py --csv-dir ./links --output-dir ${{ secrets.BASE_OUTPUT_DIR }}
    
    - name: Upload logs
      uses: actions/upload-artifact@v3
      with:
        name: instagram-downloader-logs
        path: instagram_downloader.log
        retention-days: 5 